# SRC Agent · 团队协作指南

> 目标：让多位队员一起协同开发这个「SRC 渗透测试编排 Agent」，共享上下文、规范与代码，同时守住合规红线。
> 两件事必须分开做：**代码协同走 Git**（WorkBuddy 项目不托管代码），**团队协作阵地走 WorkBuddy 项目**（共享指令/技能/资产、任务流转）。

---

## 〇、30 秒速览

- **这是什么**：对话驱动的渗透测试编排平台，调度「天狐渗透工具箱 V3.0」197 个工具（50 个可编排），面向补天 SRC 漏洞挖掘。技术栈 Python/FastAPI + 本地 Ollama(qwen3.5:9b) ReAct 决策 + SSE 前端。
- **代码在哪**：`src-agent/`（本仓库）。工具箱本体在**各自机器**（体积大、不入库），通过环境变量 `TOOLBOX_ROOT` 指定。
- **红线**：只测**已书面授权**目标；默认授权靶标 `www.jiaoyu.cn`（公益 SRC）。L2 需确认、L3 需二次确认。

---

## 一、代码侧：Git 协同（必做）

WorkBuddy「项目」≠ 代码仓库，所以代码协同用 Git（推荐私有库：GitHub / Gitee / GitLab 均可）。

### 1.1 初始化并推送（项目负责人做一次）

```bash
cd src-agent
git init
git add .
git commit -m "init: SRC 渗透编排 Agent（含 Phase 1-4 修复）"
git branch -M main
git remote add origin <你的私有仓库地址>
git push -u origin main
```

`.gitignore` 已配好，会自动排除 `__pycache__/`、`*.log`、`*.out`、`*.db`、测试产物等。

### 1.2 新队员 onboarding（每人做一次）

```bash
git clone <你的私有仓库地址> src-agent
cd src-agent

# 1) 指定自己的工具箱路径（关键，每台机器不同）
#    Windows (cmd):
set TOOLBOX_ROOT=E:\你的路径\天狐渗透工具箱-社区版V3.0
#    Windows (PowerShell):
$env:TOOLBOX_ROOT = "E:\你的路径\天狐渗透工具箱-社区版V3.0"
#    macOS/Linux:
export TOOLBOX_ROOT=/path/to/天狐渗透工具箱-社区版V3.0

# 2) 建虚拟环境并装依赖
python -m venv .venv
#    Windows: .venv\Scripts\activate      macOS/Linux: source .venv/bin/activate
pip install -r requirements.txt

# 3) 启动（需本地 Ollama 已跑 qwen3.5:9b）
python run.py --no-browser
```

依赖只有三个：`fastapi`、`uvicorn`、`httpx`。

### 1.3 提交约定

- 改 `data/tool_overrides.json` / `data/invocation_templates.json` / `app/config.py` 前，先看 `README.md` 与 `SESSION_HISTORY_SUMMARY.md`。
- 工具「禁用 / 白名单 / 模板」改动，务必在对应 JSON 里写清原因，并跑 `python test_jiaoyu.py`（或 `verify_phase4.py`）回归验证。
- 提交信息用中文 + 动词开头，如 `fix(executor): 喂入 stdin 绕过 packerfuzzer 交互提问`。

---

## 二、WorkBuddy 项目侧：建团队协作阵地（必做）

用「项目 + 任务」双层结构统一团队上下文。项目承载共享配置，任务是成员在项目里发起的会话。

### 2.1 新建项目（步骤）

1. 左侧导航栏点 **「项目」** → 左上角 **「+ 新建项目」**。
2. **项目名称**：`SRC Agent 渗透测试编排平台`。
3. **指令**：把下面 2.2 的模板整段粘贴进去。
4. 模板：可点「自定义」（或选「团队知识库 / 项目交付」再改）。
5. **连接器（可选）**：如需团队共用腾讯文档/飞书可加；SRC 开发一般不强依赖。
6. **专家（可选）**：可加安全领域专家。
7. **技能（可选）**：挂载项目级 Skill（如 `pdf`、`excel`），全员免安装即可用。
8. 点 **确定** 创建。

### 2.2 项目「指令」模板（可直接粘贴）

```
【项目背景】SRC 渗透测试编排 Agent：对话驱动的本地可控渗透平台，调度「天狐渗透工具箱 V3.0」197 个工具（50 个可编排），面向补天 SRC 漏洞挖掘。技术栈 Python/FastAPI + 本地 Ollama(qwen3.5:9b) ReAct 决策循环 + SSE 前端。

【代码】本机 src-agent/ 目录（Git 仓库）。工具箱本体在各成员机器本地，通过环境变量 TOOLBOX_ROOT 指定，不要写死路径。

【授权红线】仅允许对已获得书面授权的目标测试；默认授权靶标 www.jiaoyu.cn（公益 SRC）。风险分级 L0/L1 自动、L2 需确认、L3 需二次确认，绝不越权扫描未授权目标。

【输出规范】报告遵循补天 SRC 格式：漏洞需附复现步骤 + 证据截图 + 修复建议；结论先给证据确凿的漏洞，再扩展验证可疑点。

【协作约定】改 config/data 前先看 README.md 与 SESSION_HISTORY_SUMMARY.md；工具禁用/白名单/模板改动需在 JSON 里留注释，并跑 test_jiaoyu.py 回归验证。
```

### 2.3 上传资产库（团队知识库）

进入项目详情 → 切 **「资产」** 标签 → 上传（5GB 云端，任务执行时以 RAG 自动注入上下文）：

- `README.md`、`SESSION_HISTORY_SUMMARY.md`、`团队协作指南.md`
- `实靶测试报告-www.jiaoyu.cn.md`
- 根目录的 `工具风险分级表.md`、`天狐渗透工具箱V3.0-工具索引.md`
- 后续每次任务产出的 SRC 报告，点「归档到项目资产库」沉淀。

### 2.4 邀请成员 + 权限

1. 项目右上角 **「邀请」** → 复制链接发给队员（或分享微信 / 二维码）。
2. 队员打开链接提交加入申请，**项目管理员审批**通过。
3. 权限分层：管理员 / 普通成员，按需给。
4. 队员进入项目后，在底部输入框发起任务，**自动继承**项目指令 / 技能 / 专家 / 连接器。

### 2.5 任务协作（分享 / 转交 / 多人协作）

- **分享**：任务完成后复制链接，队员打开即可查看完整对话与产物、同步讨论。
- **转交（流转）**：点「任务流转」→ 填处理人/状态/截止日期，AI 自动生成进度摘要，打包产物 + 上下文一次性移交，接收方无缝续做。
- **多人协作**：同一任务可加多个成员共同编辑讨论。

### 2.6 项目动态

「动态」标签自动汇总：与我有关 / 成员动态 / 自动化通知，团队成员改文件、邀请成员、增删技能都有记录。

---

## 三、新人 onboarding 速查

| 步骤 | 命令 / 操作 |
|---|---|
| 1. 拉代码 | `git clone <仓库地址> src-agent` |
| 2. 指工具箱 | `set TOOLBOX_ROOT=E:\你的工具箱路径`（或 export） |
| 3. 装依赖 | `python -m venv .venv && pip install -r requirements.txt` |
| 4. 起本地模型 | 装 Ollama 并 `ollama pull qwen3.5:9b`，保持 `ollama serve` 运行 |
| 5. 启动 | `python run.py --no-browser`（默认 `127.0.0.1:8770`） |
| 6. 回归验证 | `python test_jiaoyu.py` |

---

## 四、合规红线（必须遵守）

1. **只测已书面授权的目标**；本项目默认靶标 `www.jiaoyu.cn`（公益 SRC，全部 scope 为授权测试资产）。
2. L2 漏洞验证需确认、L3 权限/横向/接管需二次确认，绝不自动越权。
3. 报告只提交**证据确凿**的漏洞；测试过程全程留痕。
4. 工具箱内含各类利用工具，仅用于授权测试与学习，禁止用于未授权系统。
